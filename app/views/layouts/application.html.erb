<!DOCTYPE html>
<html>
  <head>
    <title>School</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="icon" type="image/x-icon" href="https://cdn-icons-png.flaticon.com/512/167/167707.png">
  </head>
  <body>
    <!--  -->
    <%= render 'layouts/header' %>
    <!--  -->
    <%= yield %>
    <%= "You are logged in as - #{session[:role]} with error messages \n #{flash[:notice]}" %>
    <!--  -->
    <% flash.each do |key, value| %>
      <%= content_tag :div, value, class: "flash #{key}" %>
    <% end %>
    <!--  -->
  </body>
</html>
